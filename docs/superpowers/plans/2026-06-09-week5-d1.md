# Week 5 D1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Complete the Week 5 static-monitor deliverable by validating warm-starts, recording the required M1 GA cross-check, keeping tests green, and preparing the D1 tag.

**Architecture:** The existing Phase 1 runner already trains the M3 -> M2 -> M1/M1-hard warm-start cascade. The missing delivery behavior is to make GA verification explicit for the Week 5 target `(n=21, k=3, M1)` and to document/tag the D1 package only after tests pass.

**Tech Stack:** Python 3.10+, PyTorch, pandas, pytest, git.

---

### Task 1: Lock GA Cross-Check Semantics

**Files:**
- Modify: `tests/test_phase_outputs.py`
- Modify: `experiments/phase1_exploratory.py`

- [x] **Step 1: Write the failing test**

Add a smoke test that runs Phase 1 with `run_ga=True` on a tiny configuration and asserts the `M1` row receives `ga_f` and `ga_gap`.

- [x] **Step 2: Run the focused test to verify it fails**

Run: `python -m pytest tests/test_phase_outputs.py::test_phase1_ga_cross_check_is_recorded_on_m1_row -q`

Expected: FAIL because current code stores GA output only on `M1-hard`.

- [x] **Step 3: Implement minimal GA result routing**

Keep using the existing GA optimiser, evaluate its monitor under soft semantics for `M1`, hard semantics for `M1-hard`, and write each value to the matching row.

- [x] **Step 4: Run the focused test to verify it passes**

Run: `python -m pytest tests/test_phase_outputs.py::test_phase1_ga_cross_check_is_recorded_on_m1_row -q`

Expected: PASS.

### Task 2: Keep D1 Documentation Accurate

**Files:**
- Modify: `README.md`
- Modify: `EXECUTION_PLAN.md`

- [x] **Step 1: Document the Week 5 GA target**

Update README Phase 1 notes so the default GA check is described as the Week 5 `(n=21, k=3, M1)` cross-check.

- [x] **Step 2: Mark Week 5 checklist items complete after verification**

Update `EXECUTION_PLAN.md` only after tests and smoke runs pass, including the tag status.

### Task 3: Verify D1

**Files:**
- No source edits.

- [x] **Step 1: Run the focused tests**

Run: `python -m pytest tests/test_phase_outputs.py tests/test_reproduce_pipeline.py tests/test_experiment_entrypoints.py -q`

Expected: PASS.

- [x] **Step 2: Run the full suite**

Run: `python -m pytest -q`

Expected: PASS.

- [x] **Step 3: Run the Week 5 smoke reproduction**

Run a small Phase 1 configuration including `(n=21, k=3)` with one restart and short GA settings to verify CSV generation without spending full research compute.

Expected: `results/week5_d1_smoke/csv/phase1.csv` exists and has non-empty `M1.ga_f` for `(21,3)`.

### Task 4: Tag v0.1

**Files:**
- Git metadata only.

- [x] **Step 1: Check whether `v0.1` already exists**

Run: `git tag --list v0.1`

Expected: no output before creating the D1 tag.

- [x] **Step 2: Create an annotated tag if the working tree state can be safely represented**

If the current D1 state is committed or the user approves committing the dirty tree, run: `git tag -a v0.1 -m "D1 static monitor deliverable"`.

Expected: `git tag --list v0.1` prints `v0.1`.
